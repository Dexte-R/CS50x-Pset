In this problem set, I wrote SQL queries to extract data from the IMDb database.

Extra info:
  1. Requirements for each question is included as comments in each sql file.
  2. Solution can only contain 1 SQL query.
